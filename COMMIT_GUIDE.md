# 커밋 방법 가이드

Cursor IDE에서 Git 커밋을 수행하는 여러 가지 방법을 소개합니다.

## 방법 1: 커맨드라인 도구 사용 (가장 간단) ⭐

### 기본 사용법

1. **Cursor 터미널 열기**
   - `Ctrl + `` (백틱) 또는
   - `View > Terminal` 메뉴

2. **상태 확인**
   ```bash
   python commit_helper.py status
   ```
   - 변경된 파일이 있는지 확인
   - 브랜치 정보 확인

3. **커밋하기**
   ```bash
   python commit_helper.py commit -m "커밋 메시지"
   ```
   
   예시:
   ```bash
   python commit_helper.py commit -m "기능 추가: GitHub API 연동"
   python commit_helper.py commit -m "버그 수정"
   python commit_helper.py commit -m "문서 업데이트"
   ```

4. **커밋 + 푸시 (한 번에)**
   ```bash
   python commit_helper.py commit -m "커밋 메시지" --push
   ```

5. **커밋 로그 보기**
   ```bash
   python commit_helper.py log
   python commit_helper.py log -n 5  # 최근 5개만
   ```

### 실제 사용 예제

```bash
# 1. 현재 상태 확인
python commit_helper.py status

# 출력 예시:
# 🌿 브랜치: main
# 📊 변경사항: 3개 파일
# 
# 변경된 파일:
#   README.md
#   github_api.py
#   requirements.txt

# 2. 커밋
python commit_helper.py commit -m "프로젝트 문서 업데이트"

# 출력 예시:
# ✅ 커밋 성공
# 📝 커밋된 파일: 3개

# 3. GitHub에 푸시
python commit_helper.py commit -m "프로젝트 문서 업데이트" --push
# 또는 이미 커밋했다면:
git push
```

---

## 방법 2: Cursor의 Git UI 사용 (시각적)

### 단계별 가이드

1. **소스 제어 패널 열기**
   - 왼쪽 사이드바의 소스 제어 아이콘 클릭 (분기 모양)
   - 또는 `Ctrl + Shift + G` 단축키

2. **변경사항 확인**
   - "Changes" 섹션에서 변경된 파일 확인
   - 파일을 클릭하면 변경 내용(diff) 확인 가능

3. **스테이징 (Staging)**
   - 개별 파일: 파일 옆의 "+" 버튼 클릭
   - 전체 파일: "Changes" 옆의 "+" 버튼 클릭
   - 또는 파일을 우클릭 > "Stage Changes"

4. **커밋 메시지 입력**
   - 상단의 텍스트 박스에 커밋 메시지 입력
   - 예: "기능 추가", "버그 수정", "문서 업데이트"

5. **커밋 실행**
   - "✓" 버튼 클릭 (또는 `Ctrl + Enter`)
   - 또는 커밋 메시지 입력 후 `Ctrl + Enter`

6. **푸시 (선택사항)**
   - "..." 메뉴 클릭
   - "Push" 선택
   - 또는 `Ctrl + Shift + P` > "Git: Push" 입력

### 스크린샷 설명
```
┌─────────────────────────────────────┐
│ SOURCE CONTROL                      │
├─────────────────────────────────────┤
│ 📝 Changes (3)                      │
│   + README.md                       │
│   + github_api.py                   │
│   + requirements.txt                │
│                                     │
│ [Message (Ctrl+Enter to commit)]   │
│ ┌─────────────────────────────────┐ │
│ │ 기능 추가: GitHub API 연동      │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [✓ Commit]  [...]                  │
└─────────────────────────────────────┘
```

---

## 방법 3: Python 코드로 사용

### 기본 예제

```python
from git_auto_commit import GitAutoCommit

# Git 저장소 초기화
git = GitAutoCommit()

# 1. 상태 확인
status = git.check_status()
print(f"변경사항: {status['count']}개 파일")

if status['has_changes']:
    # 2. 자동 커밋 (변경사항이 있을 때만)
    result = git.auto_commit(
        message="자동 업데이트",
        push=False  # True로 설정하면 푸시도 함께
    )
    
    if result['success']:
        print("✅ 커밋 성공!")
        print(f"커밋된 파일: {len(result['committed_files'])}개")
    else:
        print(f"ℹ️  {result['message']}")
```

### 고급 사용

```python
from git_auto_commit import GitAutoCommit

git = GitAutoCommit()

# 특정 파일만 커밋
git.add_files(['README.md', 'config.json'])
git.commit("문서 업데이트")

# 커밋 후 푸시
git.commit("업데이트")
git.push()

# 최근 커밋 보기
commits = git.get_recent_commits(10)
for commit in commits:
    print(f"{commit['hash']} - {commit['message']}")
```

---

## 방법 4: Git 명령어 직접 사용

Cursor 터미널에서 직접 Git 명령어 사용:

```bash
# 상태 확인
git status

# 모든 변경사항 추가
git add .

# 특정 파일만 추가
git add README.md
git add src/*.py

# 커밋
git commit -m "커밋 메시지"

# 푸시
git push

# 또는 원격과 브랜치 지정
git push origin main
```

---

## 커밋 메시지 작성 팁

### 좋은 커밋 메시지 예시

✅ **좋은 예:**
```
기능 추가: GitHub API 연동 모듈
버그 수정: 인증 토큰 처리 오류 해결
문서 업데이트: README에 사용법 추가
리팩토링: 코드 구조 개선
스타일: 코드 포맷팅
```

❌ **나쁜 예:**
```
업데이트
수정
asdf
123
```

### 커밋 메시지 규칙 (선택사항)

```
타입: 간단한 설명

상세 설명 (선택사항)

타입:
- feat: 새로운 기능
- fix: 버그 수정
- docs: 문서 변경
- style: 코드 포맷팅
- refactor: 리팩토링
- test: 테스트 추가/수정
- chore: 빌드/설정 변경
```

예시:
```
feat: GitHub API 연동 기능 추가

- 사용자 정보 조회 기능
- 저장소 목록 조회 기능
- Contribution 통계 수집 기능
```

---

## 자주 묻는 질문 (FAQ)

### Q: "커밋할 변경사항이 없습니다" 메시지가 나와요
**A:** 파일을 수정하거나 새 파일을 추가하세요. 빈 커밋은 생성하지 않습니다.

### Q: 커밋은 했는데 푸시는 어떻게 하나요?
**A:** 
```bash
git push
```
또는
```bash
python commit_helper.py commit -m "메시지" --push
```

### Q: 커밋을 취소하고 싶어요
**A:**
```bash
# 마지막 커밋 취소 (변경사항은 유지)
git reset --soft HEAD~1

# 마지막 커밋 완전 삭제 (변경사항도 삭제)
git reset --hard HEAD~1
```

### Q: 여러 파일 중 일부만 커밋하고 싶어요
**A:** Cursor Git UI에서 원하는 파일만 "+" 버튼으로 스테이징하세요.

### Q: 커밋 메시지를 수정하고 싶어요
**A:**
```bash
git commit --amend -m "새로운 메시지"
```

---

## 추천 워크플로우

### 일상적인 작업

1. 파일 수정
2. `python commit_helper.py status` - 변경사항 확인
3. `python commit_helper.py commit -m "작업 내용"` - 커밋
4. 작업 완료 후 `git push` - 푸시

### 빠른 커밋

```bash
# 한 줄로 커밋 + 푸시
python commit_helper.py commit -m "업데이트" --push
```

### 여러 커밋을 한 번에 푸시

```bash
# 여러 번 커밋
python commit_helper.py commit -m "첫 번째 변경"
python commit_helper.py commit -m "두 번째 변경"

# 마지막에 한 번만 푸시
git push
```

---

## 단축키 설정 (선택사항)

Cursor에서 커밋을 더 빠르게 하려면:

1. `Ctrl + Shift + P` (명령 팔레트)
2. "Preferences: Open Keyboard Shortcuts" 입력
3. "git commit" 검색
4. 원하는 단축키 설정

또는 `keybindings.json`에 추가:
```json
{
  "key": "ctrl+alt+c",
  "command": "git.commit"
}
```

---

이제 커밋하는 방법을 모두 알았습니다! 🎉


